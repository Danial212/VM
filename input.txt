$ Decelering constant string
.you_message>Your Entered Message:
.Thank_message>Thanks for your message:
.leftCircles> You got this round left: 
.seperatorLine> ---------------------------------


LOAD R2 1

FUNC nested

SUB R11 R2
OUT -s ..leftCircles
OUT -s .seperatorLine
OUT R11

$ Recersive function
IF R11 > 0 CALL nested

RET
ENDF

LOAD R11 9
CALL nested

$ CALL INPUT
$ OUT -s .you_message
$ OUT -s #100

$ lins that start with '$' won't be compiled, like a commnet line

$ getting input with the gien size 14, and save into the address in RAM at #100

$ INP 14 #100
$ INP #100
$ INP R2

$ LABEL loop
$ SUB R1 R2
$ OUT R1
$ $ like a 7 time loop
$ IF 1 <= R1 GOTO loop

$ printign the string from the location #100

$ OUT -s #100
$ OUT -s .STR

$ the NEW conditions we can use in our 'if' blocks
$ OLD  vs   NEW
$ EQL  <->  ==
$ NEQ  <->  !=
$ LES  <->  <
$ LEQ  <->  <=
$ MOR  <->  >
$ MEQ  <->  >=

